#Defining constants the yaml files to get the path of the config.yaml 
from pathlib import Path
CONFIG_FILE_PATH = Path("config/config.yaml") # reading config yaml
PARAMS_FILE_PATH = Path("params.yaml")